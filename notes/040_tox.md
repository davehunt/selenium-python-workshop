1. Run Tox: `tox -c solutions/tox.ini`

2. Pass the `--verbose` argument: `tox -c solutions/tox.ini -- --verbose`

3. Demonstrate running environments in parallel using Detox: `detox -c solutions/tox.ini`
